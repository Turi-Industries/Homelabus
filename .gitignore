/target
**/*.rs.bk
.DS_Store

# Base d'état locale du controller
*.db
*.db-shm
*.db-wal

# 🔴 Clé maîtresse du coffre — sa fuite expose TOUS les secrets et TOUTES
# les sauvegardes. Elle ne va jamais dans git (§9quater).
hlb-master.key
*.key
!crates/**/*.key

# Artefacts de session Claude Code
.claude/RESUME.md

# Architecture notes — local only, not part of the published repository.
PLAN.md
